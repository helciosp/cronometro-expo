import React, { Component } from 'react';
import { StyleSheet, Text, View, Image, Button } from 'react-native';
import { view, img, conteine } from '../styles/index.js';

class Container extends Component{
    render(){
        return(
            <View>
         
            </View>
        );
    }
}
export default Container;